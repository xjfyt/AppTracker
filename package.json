{
  "name": "active-tracker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "setup": "npm --prefix desktop install",
    "dev": "npm --prefix desktop install && npm --prefix desktop run dev",
    "agent": "cargo run -p tracker-agent -- --api-port 5007",
    "package": "npm --prefix desktop install && cargo build -p tracker-agent --release && npm --prefix desktop run build",
    "check": "cargo fmt --check && cargo check && cargo test -p tracker-core"
  }
}

